const listaNumeros = [100, 200, 300, 400, 500, 600]; // array de numeros

for (let i = 0; i < listaNumeros.length; i++) { // percorre o array de numeros 
   listaNumeros[i]; // imprime o valor de cada posição do array e o indice da posição do array (i) que é o mesmo que o valor da posição do array
   console.log(hjlistaNumeros[i]);
}
