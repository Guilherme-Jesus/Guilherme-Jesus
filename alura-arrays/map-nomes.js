let nomes = ["joão", "MAria", "JOsé"];

const nomesAtualizados = nomes.map((nome) => nome.toUpperCase()); // transforma todos os nomes em maiúsculo
// retorna um novo array com os nomes atualizados em maiúsculo

console.log(nomesAtualizados); // ["JOÃO", "MARIA", "JOSÉ"]
